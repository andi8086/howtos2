build wavedrom-editor from aur
modify PKGBUILD to create symlink from wavedrom-editor to WaveDromEditor
install

BUNDLE_PATH=../gems bundle exec asciidoctor-pdf -r asciidoctor-diagram wd.adoc
BUNDLE_PATH=../gems bundle exec asciidoctor -r asciidoctor-diagram wd.adoc
